# Jens 扫描软件自动化测试平台 更新说明

本文件是各版本更新说明的唯一来源：每次打包时，`build.py` 会提取当前
版本段落，生成产物目录下的 `更新说明.txt`。新增版本时在此追加一个
`## vX.Y` 段落即可（也可用 `python build.py --notes "..."` 临时指定）。

约定：更新说明**简介扼要**——只写本版本“修复了什么 / 完成了什么功能”，
每条一两行短要点即可，不展开实现细节、过程与验证步骤。

## v1.10

- 后处理对比工具：人体补全新增高级参数“不生成trip，直接生成人体补全模型”，
  跳过 trip 生成，点击AI人体补全后直接选择模型底座并预览/应用。
- 修复：首次进入后处理对比页面或切换对比类型时，“专属选项组”被误弹成独立
  顶层小窗的问题；专属选项仅在打开“高级参数”弹窗时按所选类型显示。
- 修复：raptor x 的 preset 计数改为以 presets.json（21 条）为准，任务生成与
  新建向导的测试同步修正。

## v1.9

- 后处理对比工具：导入工程完成判定新增日志关键字 `i proj progress 1.000000`，
  与原 `OB_SCAN_MESSAGE_ID_PROJECT_IMPORT_SUCCESS` 任一命中即判定成功，兼容只输出该关键字的版本。

## v1.8

- 修复：`crealityscan.scan_until_frames_then_stop`（开始扫描并在帧数达标后完成）
  的停止成功判定，除原标志外，日志出现 `stop progress 1.000000`（进度到达 1.0）
  同样判定为停止完成，兼容只输出 stop progress 的机型/版本。
- 帧率统计：
  - 预览扫描后自动等待 5s 再采样，确保能稳定采到预览帧率。
  - “无标记点”按机型拆分：P1/P1S 支持交叉线/平行线两种；Pika 的“有标志点”
    分标准/均衡/快速三种，均纳入任务生成。
  - Excel 帧率数值由四舍五入改为保留整数部分。
- 后处理对比工具：
  - 发布后删除下载包由“总是自动删除”改为独立开关“发布后删除下载包”
    （默认不勾选），CLI 新增 `--delete-download-package`。
  - 修复人体补全/AI重贴图的图片选择弹窗在目录只有一张图片时可能选不中的问题。
  - 高级参数由表单内展开面板改为点击“高级参数...”按钮弹出设置弹窗，保存/取消
    后自动刷新摘要。
  - 高斯渲染新增质量等级选择（快速/标准/高质量，默认高质量），CLI 新增
    `--gaussian-quality` 参数。
  - 新增任务配置自动保留：关闭工具后下次打开恢复上一次配置
    （保存到 `%LOCALAPPDATA%\Jens\后处理对比工具\settings.json`）。

## v1.7

- 后处理对比工具新增“开启Charles”开关：任务配置区提供开关与 `Charles.exe`
  路径选择；勾选开启时会弹出文件选择对话框指定 `Charles.exe`（已设置过路径
  则不重复弹出，可通过“浏览”随时更换）。
- 开启 Charles 后，在发布版全部后处理完成并关闭、启动测试版之前，自动以独立
  进程拉起 Charles 抓包代理，用于抓取测试版流量；CLI 新增 `--charles-exe`
  参数。
- 若 Charles 启动失败（路径不存在、进程无法拉起），本次对比直接判定失败并
  不再启动测试版，避免在未抓包的情况下空跑整个测试版流程。
- 新增 `tests/test_postprocess_charles.py` 覆盖 CLI 参数、Charles 启动/失败
  分支、GUI 开关交互、校验与 `--charles-exe` 透传。

## v1.6

- 将 `工具/标定分数查看工具` 与 `工具/固件升级工具` 沉淀进平台内部 Steps
  （`steps/tool/`）：
  - 新增 `tool.calibration_score.read`（读取标定分数）：自包含实现，从
    CrealityScan 最新日志解析设备名/SN/标定板/标定分数（normal/raptor/
    ferret 三种格式）/固件版本，日志目录优先级为
    `params.log_dir` > 用例 `app.log_dir` > 当前用户默认日志目录。
  - 新增 `tool.firmware_upgrade.stream`（固件开流操作）：复用源工具
    StreamHandler，按设备模组自动路由开流（Pika 专用 / 蓝色线激光双流 /
    散斑单流），等待开流、帧数达标并返回首页。
  - 新增 `tool.firmware_upgrade.upgrade`（固件升级操作）：复用源工具
    FirmwareHandler，进入设置→设备管理→选择固件文件→确认升级，按设备
    模组/连接方式等待升级成功并验证固件版本，返回首页。
  - 固件两个 Step 运行时从 `工具/固件升级工具` 定位源实现（或环境变量
    `JENS_FIRMWARE_UPGRADE_ROOT`），日志通过 `params.log_dir` /
    用例 `app.log_dir` 透传；Airtest 依赖仅在 `run()` 内加载，不影响平台
    Step 扫描与导入。
- 平台 Step 总数由 37 增至 40；新增 `tests/test_tool_steps_deposit.py`
  覆盖 Step 扫描、导入、标定分数解析与固件工具目录解析。

## v1.5

- 修复测试版缺包自动下载实际不生效的缺陷：平台流程把后处理 Step 的
  `version_label` 传成了带工程序号的 `step_label`（如“测试版工程1”），而
  三个 Step（人体补全/AI重贴图/高斯渲染）的自动下载判断要求精确等于
  “测试版”，导致判断永远不成立、卡在下载弹窗。现改为单独传入真正的
  阶段标签（发布版/测试版），自动下载恢复正常；已用测试版高斯渲染实测
  验证：自动点击“下载”→309MB 包下载完成（AITextureRestore 1.1.10）→
  渲染 768s → 返回首页 → 正常关闭，全流程通过。
- 日志输出重构：GUI 日志窗口由“最近 5 行”扩大为保留最近 2000 行；CLI
  输出增加时间戳、阶段横幅与每阶段耗时；本次运行的完整 stdout/stderr
  会同时写入输出目录下的 `运行日志.txt`，卡住时先看该文件最后几行即可
  定位卡在哪个环节。
- 各后处理 Step 的长等待（启动主窗口、导入工程、AI重贴图、高斯渲染、
  人体补全的进度条出现/消失）统一改为带“心跳”打印的轮询：每 30s 打印
  一次已等待时长与上限，避免长等待看起来像“无响应”，也方便区分“仍在
  等待”与“真的卡死”。
- 修复 AI重贴图 `_wait_progress_disappear` 中一处 `wait(模板)` 未设超时、
  可能无限静默等待的问题。
- 后处理对比 CLI 新增 `--test-only` 参数：只跑测试版（跳过发布版阶段与
  最终对比表），用于单独验证测试版流程（如缺包自动下载）。

## v1.4

- 后处理对比工具 Excel 对比表表头信息行附带对应操作下载包版本号：人体补全
  读取 `AIBodyComplete`、AI重贴图/高斯渲染读取 `AITextureRestore` 的
  `version.txt`，缺失时显示“未获取”，贴图无独立下载包不显示。
- 发布版全部后处理完成后自动删除本次操作对应的下载包（`Extensions` 下
  `<包名>` 及其 `<包名>_Data` 目录），使测试版运行时重新下载新版本下载包，
  避免测试版误用发布版携带的旧包。（自 v1.8 起改为用户可选的独立开关
  “发布后删除下载包”，默认不勾选。）
- 人体补全、AI重贴图、高斯渲染三个 Step 内置测试版缺包自动下载：当测试版
  且本地无对应 `version.txt` 时，自动点击 CrealityScan 的“下载”弹窗按钮并
  等待下载完成（以对应 `version.txt` 出现为准，下载进度从 `app.log` 读取
  打印），完成后继续后续流程。其中高斯渲染的下载弹窗出现在点击“应用”按钮
  之后，人体补全在点击“AI人体补全”按钮后，AI重贴图在点击“AI重贴图”按钮后。

## v1.3

- 后处理对比工具新增“人体补全”操作并沉淀进平台：新增
  `human_body_completion` Step，支持在 `Select Head Front Image` 弹窗中全选
  `img/` 目录图片导入、可选开启超清几何精度、双进度条等待与截图留证。
- 后处理对比工具 CLI/UI 新增 `--human-body-completion-timeout`、
  `--human-body-hd-geometry`、`--base-wait` 参数。
- 新增五个滑轨任务：Ferret开流、P1开流usb、Raptor开流、S1开流wifi、
  X1后处理usb。
- `switch_position` 位置预设统一为负脉冲：小物体 -30000、中物体 -170000、
  人脸 -370000、大物体 -580000；演示任务切至小物体位置。

## v1.2

- 修复打包版启动崩溃：`ImportError: loader for motion_service cannot handle _main`。
- 原因：`_run_slide_rail_script` 把模块 `__name__` 改为 `__main__` 后再
  `exec_module`，触发 CPython 加载器 `_check_name` 名称校验失败；该路径仅在
  打包态（`--slide-rail-service-runner`）触发，源码态不受影响。
- 修复：改用 `runpy.run_path(..., run_name="__main__")` 直接编译执行，绕开
  加载器名称校验，同时保持 `__file__` 指向 `滑轨/`（zmotion_controller 用
  它回退定位 DLL）。
- 验证：源码态与冻结态下 motion_service 均正常启动，5000/5001 端口监听。

## v1.1

- 首次将 滑轨/ 运动控制接入打包产物（该版本存在启动崩溃，由 v1.2 修复）。
