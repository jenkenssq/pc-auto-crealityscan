{
  "id": "crealityscan.package_operation",
  "name": "进行封装操作",
  "version": "1.0.0",
  "description": "来自现有 Airtest 用例：进行封装操作（含进度条消失轮询）。",
  "params_schema": {
    "timeout_sec": {
      "type": "number",
      "default": 60,
      "description": "等待进度条消失的超时（秒）。"
    }
  }
}

