该工具是为了对比不同版本的CrealityScan软件对于相同工程的后处理效果的对比（贴图，重贴图，人体补全，高斯渲染之类的）

目前就是采用现在的自动化平台的执行内核，具体的步骤setp由我之后给你

流程是这样的，主要是对比发布版本和现在的测试版本的后处理效果，这两个版本的exe路径由用户选择，操控打开后，软件有导入工程的功能，这些原始工程的路径也是由用户提供。工程打开后，由用户选择进行哪些setp后处理操作，处理完后截屏，并且把处理完的整个工程复制一份，更名保存在另一个文件夹

先打开旧版本（也就是发布版本），处理完后再打开新版本（现测试版本）处理

