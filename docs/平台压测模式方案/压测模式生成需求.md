目前平台的任务遇到报错会自动停止，需要等待人工处理错误，这也是正确的，不过还要一种场景，可能某个模组的某个模式需要开流，后处理压测，压测次数在50次以上这种，原本的正常工作模式就不太符合了。所以我想要在平台开设一个压测模式，点击切换后，弹窗选择Crealityscan的exe路径，选择任务执行多少次。

压测模式就是加多几个异常处理的场景，比如Crealityscan软件卡死了的话，截屏保存，当次日志保存，然后强制杀死进程，重新打开Crealityscan软件，再进行任务。这种情况下重新打开软件会有日志上报弹窗需要处理，这是和正常工作模式不一样的地方。